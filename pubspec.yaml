name: skills_audit_system
description: A comprehensive skills audit and management system for National Treasury

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase dependencies
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_storage_web: ^3.6.21

    # Chart library for analytics
  fl_chart: ^0.66.2

  # UI and Design
  cupertino_icons: ^1.0.2

  # Utilities
  intl: ^0.20.2
  shared_preferences: ^2.2.2

  # Network
  http: ^1.1.2

  # Utilities
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Assets (create these directories if you want to add assets later)
  # assets:
  #   - assets/images/
  #   - assets/icons/